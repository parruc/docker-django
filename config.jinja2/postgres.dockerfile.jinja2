FROM postgres:latest
{% if gis %}
COPY config/postgres.entrypoint.sql /docker-entrypoint-initdb.d/
RUN apt update && apt install -y postgis
{% endif %}

{% if development %}
RUN mkdir -p /var/lib/postgresql/data
RUN chmod -R 777 /var/lib/postgresql/data
{% endif %}
